pip install streamlit
pip install transformers
pip install torch / gpu 사용 가능하면 pip install torch --index-url https://download.pytorch.org/whl/cu118
pip install googletrans==4.0.0-rc1 (최신버전은 깨져있음 2025-11-27기준)
pip install accelerate (모델 로딩 중 경고 방지 / 속도 개선)